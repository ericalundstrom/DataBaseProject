<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Conference Management</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Welcome reviewer, <%= user.first_name %> <%= user.last_name %>!</h1>
    <p>Here you can see all articles assigned to you for review.</p>

    <a href="/">
        <button type="button" class="logout-button">Logout</button>
    </a>

    <table>
        <thead>
        <tr>
            <th>Title</th>
            <th>Article Type</th>
            <th>Keywords</th>
            <th>Status</th>
            <th>Submission Date</th>
        </tr>
        </thead>
        <tbody>
            <% articles.forEach(function(article) { %>
                <tr>
                    <td>
                        <a href="#" class="article-title" data-title="<%= article.title %>" data-content="<%= article.content %>">
                            <%= article.title %>
                        </a>
                    </td>
                    <td><%= article.article_type %></td>
                    <td><%= article.keywords %></td>
                    <td><%= article.article_status %></td>
                    <td><%= article.submission_date.toLocaleDateString() %></td>
                    <td>
                        <select>
                            <option value="">Please select</option>
                            <option value="accept">Accept</option>
                            <option value="reject">Reject</option>
                        </select>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
</body>
</html>
